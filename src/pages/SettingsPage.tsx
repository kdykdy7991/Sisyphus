import {useEffect,useState} from 'react';import {settingsService} from '../services/mockServices';import type {Settings} from '../types';import {Database,Download,RotateCcw,Eye,EyeOff,FolderOpen,Check,LoaderCircle} from '../components/Icons';import {Button,Input} from '../components/UI';
export function SettingsPage(){const[s,setS]=useState<Settings>();const[show,setShow]=useState(false);const[state,setState]=useState('');useEffect(()=>{settingsService.get().then(setS)},[]);if(!s)return null;const set=(k:keyof Settings,v:string)=>setS({...s,[k]:v});const test=async()=>{setState('testing');await settingsService.testConnection();setState('ok')};const save=async()=>{await settingsService.save(s);setState('saved');setTimeout(()=>setState(''),1500)};return <div className="settings-page page-pad"><header className="page-header"><div><p>设置</p><h1>配置你的 Interview Kit</h1><span>Phase 1 中的配置仅保存在 Mock 状态。</span></div><Button variant="primary" onClick={save}>{state==='saved'?<><Check/>已保存</>:<>保存设置</>}</Button></header><div className="settings-content"><section><div className="settings-heading"><h2>模型</h2><p>配置用于对话与图片提取的模型服务。</p></div><div className="form-grid"><label>API Base URL<Input value={s.apiBaseUrl} onChange={e=>set('apiBaseUrl',e.target.value)}/></label><label>API Key<div className="password"><Input type={show?'text':'password'} value={s.apiKey} placeholder="sk-••••••••••••" onChange={e=>set('apiKey',e.target.value)}/><button onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><label>Chat Model<Input value={s.chatModel} onChange={e=>set('chatModel',e.target.value)}/></label><label>Vision Model<Input value={s.visionModel} onChange={e=>set('visionModel',e.target.value)}/></label><div/><Button onClick={test}>{state==='testing'?<><LoaderCircle className="spin"/>测试中</>:state==='ok'?<><Check/>连接正常</>:'Test Connection'}</Button></div></section><section><div className="settings-heading"><h2>数据</h2><p>管理本地数据库位置与数据导出。</p></div><div className="data-location"><label>Database Location<Input value={s.databaseLocation} onChange={e=>set('databaseLocation',e.target.value)}/></label><Button><FolderOpen/>选择位置</Button></div><div className="data-actions"><button><Database/><span><b>Export Backup</b><small>导出完整数据库备份</small></span><Download/></button><button><RotateCcw/><span><b>Restore Backup</b><small>从备份文件恢复数据</small></span><FolderOpen/></button><button><Download/><span><b>Export Markdown</b><small>导出为可阅读的 Markdown</small></span><Download/></button><button><Download/><span><b>Export JSON</b><small>导出结构化 JSON 数据</small></span><Download/></button></div></section><aside><h3>关于本阶段</h3><p>当前版本使用 TypeScript Mock Service。模型测试、导入导出与数据库位置不会触发真实后端操作。</p><small>Interview Kit · Phase 1<br/>Desktop UI Prototype</small></aside></div></div>}
