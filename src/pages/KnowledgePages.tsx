import {useEffect,useMemo,useState} from 'react';import {useNavigate,useParams} from 'react-router-dom';import {useApp} from '../AppContext';import {knowledgeService} from '../services';import type {Knowledge} from '../types';import {Search,ChevronRight,ChevronLeft,FileText,Star,PenLine,MoreHorizontal,Plus,Save,Tags,FolderOpen,Clock} from '../components/Icons';import {Button,Input,Tag,Textarea,Empty,Spinner} from '../components/UI';
const renderAnswer=(text:string)=>text.split('\n').map((line,i)=>line.startsWith('## ')?<h2 key={i}>{line.slice(3)}</h2>:line?<p key={i}>{line}</p>:<br key={i}/>);
export function KnowledgePage(){const{knowledge}=useApp();const nav=useNavigate();const[q,setQ]=useState('');const[domain,setDomain]=useState('全部');const domains=['全部',...new Set(knowledge.map(x=>x.domain))];const topics=[...new Set(knowledge.filter(x=>domain==='全部'||x.domain===domain).map(x=>x.topic))];const filtered=knowledge.filter(x=>(domain==='全部'||x.domain===domain)&&[x.question,x.answer,x.topic,...x.tags].join(' ').toLowerCase().includes(q.toLowerCase()));return <div className="knowledge-browser"><aside className="topic-panel"><h1>知识库</h1><div className="tabs"><b>按主题</b><span>按标签</span><span>按收藏</span></div><div className="domain-list">{domains.map(d=><button className={domain===d?'active':''} onClick={()=>setDomain(d)} key={d}><ChevronRight/>{d}<small>{d==='全部'?knowledge.length:knowledge.filter(x=>x.domain===d).length}</small></button>)}{topics.map(t=><button className="topic" key={t} onClick={()=>setQ(t)}>{t}<small>{knowledge.filter(x=>x.topic===t).length}</small></button>)}</div><Button variant="ghost"><Plus/> 新建主题</Button></aside><section className="knowledge-list"><header><div><p>{domain}</p><h2>{q?`“${q}” 的搜索结果`:'全部知识'}</h2></div><div className="small-search"><Search/><input placeholder="搜索知识库…" value={q} onChange={e=>setQ(e.target.value)}/></div></header>{filtered.length?<div className="knowledge-rows">{filtered.map(x=><button key={x.id} onClick={()=>nav(`/knowledge/${x.id}`)}><FileText/><span><b>{x.question}</b><small>{x.answer.slice(0,82)}…</small><em>{x.tags.map(t=><i key={t}>{t}</i>)}</em></span><div><small>{x.domain} / {x.topic}</small><ChevronRight/></div></button>)}</div>:<Empty icon={<Search/>} heading="没有找到知识" text="尝试更换关键词或主题。"/>}</section></div>}
export function KnowledgeDetailPage(){const{id}=useParams();const{knowledge,refresh}=useApp();const nav=useNavigate();const[item,setItem]=useState<Knowledge>();const[editing,setEditing]=useState(false);const[draft,setDraft]=useState<Knowledge>();useEffect(()=>{knowledgeService.get(id!).then(x=>{setItem(x);setDraft(x)})},[id,knowledge]);const related=useMemo(()=>item?knowledge.filter(x=>item.relatedIds.includes(x.id)||x.topic===item.topic&&x.id!==item.id).slice(0,6):[],[item,knowledge]);if(!item||!draft)return <div className="center"><Spinner/></div>;const save=async()=>{await knowledgeService.save({...draft,updatedAt:new Date().toLocaleString()});await refresh();setItem(draft);setEditing(false)};return <div className="detail"><div className="detail-top"><button onClick={()=>nav('/knowledge')}><ChevronLeft/> 知识库</button><span>{item.domain}</span><ChevronRight/><span>{item.topic}</span><ChevronRight/><b>{item.question}</b></div><article className="reader"><div className="reader-actions"><small># {knowledge.findIndex(x=>x.id===item.id)+1}</small><span/><button onClick={()=>setItem({...item,favorite:!item.favorite})}><Star fill={item.favorite?'currentColor':'none'}/> {item.favorite?'已收藏':'收藏'}</button><button onClick={()=>setEditing(!editing)}><PenLine/> 编辑</button><button><MoreHorizontal/></button></div>{editing?<div className="edit-form"><label>问题<Input value={draft.question} onChange={e=>setDraft({...draft,question:e.target.value})}/></label><label>核心回答<Textarea rows={16} value={draft.answer} onChange={e=>setDraft({...draft,answer:e.target.value})}/></label><div className="split"><label>领域<Input value={draft.domain} onChange={e=>setDraft({...draft,domain:e.target.value})}/></label><label>主题<Input value={draft.topic} onChange={e=>setDraft({...draft,topic:e.target.value})}/></label></div><label>标签<div className="tag-editor">{draft.tags.map(t=><Tag key={t} onRemove={()=>setDraft({...draft,tags:draft.tags.filter(x=>x!==t)})}>{t}</Tag>)}<button onClick={()=>setDraft({...draft,tags:[...draft.tags,'新标签']})}><Plus/>添加</button></div></label><div className="form-actions"><Button variant="ghost" onClick={()=>{setDraft(item);setEditing(false)}}>取消</Button><Button variant="primary" onClick={save}><Save/>保存更改</Button></div></div>:<><h1>{item.question}</h1><div className="tag-row">{item.tags.map(t=><Tag key={t}>{t}</Tag>)}<button><Plus/></button></div><div className="answer"><h2>1. 核心回答</h2><div className="lead">{item.answer.split('\n')[0]}</div>{renderAnswer(item.answer.split('\n').slice(1).join('\n'))}<hr/><h2>2. 可能的追问</h2><ul>{item.followUps.map(x=><li key={x}>{x}</li>)}</ul></div></>}</article><aside className="detail-rail"><div className="small-search"><Search/><input placeholder="搜索知识库…"/></div><section><h3>相关问题 <small>{related.length}</small></h3>{related.map(x=><button onClick={()=>nav(`/knowledge/${x.id}`)} key={x.id}><FileText/>{x.question}</button>)}</section><section><h3>所属主题</h3><p><FolderOpen/> {item.domain}　›　{item.topic}</p><p><Tags/> {item.tags.length} 个标签</p></section><section><h3>来源</h3><p>{item.source}</p></section><section><h3>备注</h3><Textarea placeholder="记录一些额外的想法…" rows={4}/></section><div className="meta"><span><Clock/> 创建时间</span>{item.createdAt}<span>最后编辑</span>{item.updatedAt}</div></aside></div>}
